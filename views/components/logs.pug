table(class='table table-striped table-dark table-hover caption-top table-sm table-striped table-bordered')
    caption
        button(hx-on:click="document.getElementById('logs').innerHTML=''" class="btn btn-outline-primary")="Clear"
    thead
        tr
            th.text-secondary(scope="col")="Level"
            th.text-secondary(scope="col")="Time"
            th.text-secondary(scope="col")="Source"
            th.text-secondary(scope="col")="Log"
    tbody#logs
